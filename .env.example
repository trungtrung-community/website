# The waitlist provider. Without these, lib/waitlist.ts logs in development and
# fails loudly in production rather than dropping an address silently.
RESEND_API_KEY=
RESEND_AUDIENCE_ID=

# Where the design system lives, if not ../design-system.
# TRUNGTRUNG_DS_PATH=/path/to/design-system
